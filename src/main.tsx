import React from "react";
import ReactDOM from "react-dom/client";
import EmmanuelPortfolio from "./App";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <EmmanuelPortfolio />
  </React.StrictMode>
);
